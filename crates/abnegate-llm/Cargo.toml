[package]
name = "abnegate-llm"
description = "OpenAI-compatible chat completions with SSE streaming and weighted provider routing, modality-axis provider traits, cost estimation, local hardware profiling and model catalogue browsing"
keywords = ["llm", "ai", "openai", "router", "catalog"]
categories = [
    "api-bindings",
    "asynchronous",
    "web-programming",
    "web-programming::http-client",
]
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[features]
default = []
anthropic = ["tokio/io-util", "tokio/macros", "tokio/process"]
catalog = ["dep:regex", "dep:scraper"]
download = ["tokio/fs", "tokio/sync"]
google = []
openai = ["dep:base64", "reqwest/multipart", "tokio/fs"]
testing = []

[dependencies]
abnegate-http = { path = "../abnegate-http", version = "0.1.0" }
abnegate-secret = { path = "../abnegate-secret", version = "0.1.0" }
async-stream = { workspace = true }
async-trait = { workspace = true }
base64 = { workspace = true, optional = true }
futures = { workspace = true }
rand = { workspace = true }
regex = { workspace = true, optional = true }
reqwest = { workspace = true, features = ["json", "stream"] }
scraper = { workspace = true, optional = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["rt", "time"] }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true, features = ["io-util", "macros", "net", "test-util"] }
wiremock = { workspace = true }
